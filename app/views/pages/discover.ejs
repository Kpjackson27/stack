<!DOCTYPE html>
<html lang="en">
<head>
	<% include ../partials/head %>
</head>
<body>
	<ul>
		<% for (var i = 0; i< poems.length; i++) {%>
			<li>
				<%= poems.title %>
				br
				<%= 'Posted: ' + poems.date %>
				br
				<%= poems.description %>
			</li>
		<% } %>
	</ul>
</body>
</html>